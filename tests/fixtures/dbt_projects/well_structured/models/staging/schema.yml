version: 2

models:
  - name: stg_customers
    description: Staging table for customer data from the raw CRM system
    columns:
      - name: customer_id
        description: Unique identifier for each customer
        tests:
          - unique
          - not_null
      - name: customer_name
        description: Full name of the customer
