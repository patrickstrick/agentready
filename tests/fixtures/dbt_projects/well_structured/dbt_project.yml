name: 'well_structured'
config-version: 2
profile: 'default'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

require-dbt-version: ">=1.0.0"

models:
  well_structured:
    materialized: view
